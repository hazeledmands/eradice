# Supply-chain hardening: never run dependency lifecycle scripts on install.
# This project uses Yarn (see .yarnrc.yml `enableScripts: false`); this file
# guards against arbitrary code execution if anyone uses npm/npx instead.
ignore-scripts=true
